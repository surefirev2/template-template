# Template sync config schema

Schema and behavior for `.github/template-sync.yml` are defined in the action repo:

**[repo-sync-action: template-sync-config-schema.md](https://github.com/surefirev2/repo-sync-action/blob/main/docs/template-sync-config-schema.md)**

That document covers `repositories`, `include_paths`, `exclude_paths`, `repo_include_paths`, and allowlist vs blacklist behavior.
